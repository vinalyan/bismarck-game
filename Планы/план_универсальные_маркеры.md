# План реализации универсальной системы маркеров гексов

## Цели:
1. Поддержка нескольких маркеров одного типа в одном гексе
2. Поддержка разных типов маркеров (поиск, воздушная атака, и т.д.)
3. Упрощение синхронизации данных между фронтендом и бэкендом
4. Расширяемость для будущих типов маркеров

---

## Этап 1: База данных

### 1.1. Создать миграцию для универсальной таблицы `hex_markers`

**Файл:** `backend/migrations/020_create_hex_markers.sql`

```sql
-- Универсальная таблица для всех типов маркеров на гексах
CREATE TABLE IF NOT EXISTS hex_markers (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    game_id UUID NOT NULL REFERENCES games(id) ON DELETE CASCADE,
    player_id UUID NOT NULL,
    hex_id VARCHAR(10) NOT NULL,
    marker_type VARCHAR(20) NOT NULL, -- 'flight_path_search', 'air_attack', etc.
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- Индексы для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_hex_markers_game_hex_type 
    ON hex_markers(game_id, hex_id, marker_type);
    
CREATE INDEX IF NOT EXISTS idx_hex_markers_game_player_type 
    ON hex_markers(game_id, player_id, marker_type);

CREATE INDEX IF NOT EXISTS idx_hex_markers_game_type 
    ON hex_markers(game_id, marker_type);

-- Комментарии
COMMENT ON TABLE hex_markers IS 'Универсальная таблица для хранения маркеров на гексах. Поддерживает несколько маркеров одного типа в гексе.';
COMMENT ON COLUMN hex_markers.marker_type IS 'Тип маркера: flight_path_search (путь полета поиска), air_attack (воздушная атака) и т.д.';
```

### 1.2. Миграция данных из старой таблицы

**Файл:** `backend/migrations/021_migrate_flight_path_markers.sql`

```sql
-- Перенос данных из flight_path_search_markers в hex_markers
INSERT INTO hex_markers (game_id, player_id, hex_id, marker_type, created_at, updated_at)
SELECT game_id, player_id, hex_id, 'flight_path_search', created_at, updated_at
FROM flight_path_search_markers
ON CONFLICT DO NOTHING;

-- После проверки можно удалить старую таблицу (опционально)
-- DROP TABLE IF EXISTS flight_path_search_markers;
```

### 1.3. Добавить миграцию в список

**Файл:** `backend/cmd/migrate/main.go`
- Добавить `020_create_hex_markers` и `021_migrate_flight_path_markers` в список миграций

---

## Этап 2: Backend - SearchService

### 2.1. Обновить методы работы с маркерами

**Файл:** `backend/internal/game/services/search_service.go`

#### Методы для работы с новой таблицей:

```go
// GetHexMarkers возвращает все маркеры указанного типа для игрока в игре
func (s *SearchService) GetHexMarkers(gameID, playerID string, markerType string) ([]string, error)

// AddHexMarker добавляет маркер указанного типа в гекс
func (s *SearchService) AddHexMarker(gameID, playerID, hexID, markerType string) error

// RemoveHexMarker удаляет один маркер указанного типа из гекса
func (s *SearchService) RemoveHexMarker(gameID, playerID, hexID, markerType string) error

// GetHexMarkersCount возвращает количество маркеров каждого типа в гексе
func (s *SearchService) GetHexMarkersCount(gameID, hexID string, playerSide string) (map[string]int, error)
// Возвращает: {"flight_path_search": 2, "air_attack": 1}

// getHexMarkersInHex возвращает маркеры указанного типа в гексе (для расчета факторов)
func (s *SearchService) getHexMarkersInHex(gameID, hexID string, playerSide string, markerType string) ([]string, error)
```

#### Обновить CalculateSearchFactors:

```go
// Использовать getHexMarkersInHex вместо getFlightPathMarkersInHex
flightPathMarkers, err := s.getHexMarkersInHex(gameID, hexID, searchingPlayerSide, "flight_path_search")
```

### 2.2. Обновить AdminPhaseHandler

**Файл:** `backend/internal/game/services/phase_handlers.go`

```go
// Удалять маркеры по типу
err := h.searchService.RemoveAllHexMarkers(gameID, "flight_path_search")
```

---

## Этап 3: Backend - API Handlers

### 3.1. Расширить SearchHandler

**Файл:** `backend/internal/api/handlers/search_handler.go`

#### Новые методы:

```go
// GetHexMarkers - универсальный метод для получения маркеров любого типа
func (h *SearchHandler) GetHexMarkers(w http.ResponseWriter, r *http.Request)
// GET /api/games/{gameId}/hex-markers?type=flight_path_search

// AddHexMarker - универсальный метод для добавления маркера
func (h *SearchHandler) AddHexMarker(w http.ResponseWriter, r *http.Request)
// POST /api/games/{gameId}/hex-markers
// Body: {"hex_id": "F26", "marker_type": "flight_path_search"}

// RemoveHexMarker - универсальный метод для удаления маркера
func (h *SearchHandler) RemoveHexMarker(w http.ResponseWriter, r *http.Request)
// DELETE /api/games/{gameId}/hex-markers/{hexId}?type=flight_path_search
```

#### Расширить GetSearchFactorsByHexes:

```go
// В ответ добавить информацию о маркерах
type HexSearchInfo struct {
    Factors           int            `json:"factors"`
    MarkersByType     map[string]int `json:"markers_by_type"` // {"flight_path_search": 2, "air_attack": 1}
}

response := map[string]interface{}{
    "hex_factors": hexFactors,
    "hex_markers": hexMarkersInfo, // Map<hexId, HexSearchInfo>
}
```

---

## Этап 4: Frontend - API Client

### 4.1. Обновить searchAPI.ts

**Файл:** `frontend/src/services/api/searchAPI.ts`

```typescript
// Новые типы
export interface HexMarkers {
  flight_path_search?: number;
  air_attack?: number;
  // можно расширять
}

export interface HexSearchData {
  factors: number;
  markers: HexMarkers;
}

export interface SearchFactorsResponse {
  hex_factors: Record<string, number>;
  hex_markers: Record<string, HexMarkers>; // hexId -> маркеры
}

// Универсальные методы для работы с маркерами
addHexMarker: async (gameId: string, hexId: string, markerType: string, token: string)
removeHexMarker: async (gameId: string, hexId: string, markerType: string, token: string)
getHexMarkers: async (gameId: string, markerType: string, token: string): Promise<string[]>
```

### 4.2. Обновить getSearchFactors

```typescript
getSearchFactors: async (...): Promise<SearchFactorsResponse>
// Теперь возвращает и факторы, и маркеры
```

---

## Этап 5: Frontend - Компоненты

### 5.1. Обновить Game.tsx

**Файл:** `frontend/src/components/Game.tsx`

```typescript
// Заменить Set<string> на структуру с количеством
interface HexMarkersData {
  [hexId: string]: {
    flight_path_search: number;
    air_attack?: number;
  };
}

const [hexMarkers, setHexMarkers] = useState<HexMarkersData>({});

// Загружать из ответа search/factors вместо отдельного API
// В useEffect для calculateSearchFactors:
const response = await searchAPI.getSearchFactors(...);
if (response.hex_markers) {
  setHexMarkers(response.hex_markers);
}
```

### 5.2. Обновить HexMap.tsx

**Файл:** `frontend/src/components/HexMap.tsx`

```typescript
interface HexMapProps {
  // Заменить
  // flightPathSearchMarkers?: Set<string>;
  // На
  hexMarkers?: Record<string, HexMarkers>;
}

// В renderHexes:
const flightPathSearchCount = hexMarkers?.[hexId]?.flight_path_search || 0;
const hasFlightPathMarker = flightPathSearchCount > 0;

// Передавать количество для отображения нескольких иконок
<Hex
  flightPathMarkerCount={flightPathSearchCount}
  // или
  hasFlightPathMarker={hasFlightPathMarker}
/>
```

### 5.3. Обновить Hex.tsx

**Файл:** `frontend/src/components/Hex.tsx`

```typescript
interface HexProps {
  // Если несколько маркеров - можно отображать несколько иконок или число
  flightPathMarkerCount?: number; // 0, 1, 2, ...
  // или оставить boolean если достаточно знать наличие
  hasFlightPathMarker?: boolean;
}

// Для отображения нескольких иконок (если нужно):
{flightPathMarkerCount > 0 && (
  <g className="flight-path-markers">
    {Array.from({ length: Math.min(flightPathMarkerCount, 3) }).map((_, i) => (
      <image
        key={i}
        href="/assets/markers/FP.svg"
        x={iconX + i * 3} // небольшой сдвиг для нескольких иконок
        y={iconY + i * 3}
        width={iconSize}
        height={iconSize}
        preserveAspectRatio="xMidYMid meet"
        style={{ pointerEvents: 'none' }}
      />
    ))}
    {flightPathMarkerCount > 3 && (
      <text x={iconX} y={iconY + iconSize} fontSize="10" fill="white">
        +{flightPathMarkerCount - 3}
      </text>
    )}
  </g>
)}
```

---

## Этап 6: Обновление существующего кода

### 6.1. Обновить вызовы API

- Заменить `addFlightPathSearchMarker` на `addHexMarker(..., "flight_path_search")`
- Заменить `getFlightPathSearchMarkers` на использование данных из `getSearchFactors`

### 6.2. Убрать старые методы

- Удалить методы, специфичные для `flight_path_search_markers`
- Оставить только универсальные методы для работы с `hex_markers`

---

## Этап 7: Подготовка к воздушным атакам

### 7.1. Создать константы типов маркеров

**Файл:** `backend/internal/game/models/markers.go`

```go
const (
    MarkerTypeFlightPathSearch = "flight_path_search"
    MarkerTypeAirAttack        = "air_attack"
    // можно расширять
)
```

### 7.2. Подготовить структуру для воздушных атак

- API методы уже будут готовы (универсальные)
- Нужно будет только добавить UI для размещения маркеров воздушной атаки

---

## Порядок реализации:

1. ✅ Создать миграцию и таблицу `hex_markers`
2. ✅ Мигрировать данные из старой таблицы
3. ✅ Обновить SearchService для работы с новой таблицей
4. ✅ Обновить API handlers
5. ✅ Расширить ответ `/search/factors` информацией о маркерах
6. ✅ Обновить фронтенд для использования данных из `/search/factors`
7. ✅ Убрать отдельные запросы для маркеров
8. ✅ Обновить UI для отображения нескольких маркеров (опционально)

---

## Преимущества нового подхода:

1. ✅ Единый источник данных (маркеры приходят вместе с факторами)
2. ✅ Автоматическая синхронизация
3. ✅ Поддержка нескольких маркеров одного типа
4. ✅ Легкое добавление новых типов маркеров
5. ✅ Упрощение кода (меньше отдельных запросов)
6. ✅ Меньше гонок данных (race conditions)

